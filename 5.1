#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int n;
    cout << "Podaj liczbe elementow wektora: ";
    cin >> n;

    vector<int> vec;

    cout << "Podaj " << n << " elementow: " << endl;
    for (int i = 0; i < n; ++i) {
        int value;
        cin >> value;
        vec.push_back(value);
    }

    auto max_element_it = max_element(vec.begin(), vec.end());
    int max_value = *max_element_it;
    int max_index = distance(vec.begin(), max_element_it);

    cout << "Maksymalny element wektora: " << max_value << endl;
    cout << "Indeks maksymalnego elementu: " << max_index << endl;

    return 0;
}
